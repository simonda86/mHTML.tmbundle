=== INSTALLATION ===

cd ~/Library/"Application Support"/TextMate/Bundles/

If that directory wasn't found, you'll need to create it, then:

git clone git://github.com/simonda86/mHTML.tmbundle.git "mHTML.tmbundle"